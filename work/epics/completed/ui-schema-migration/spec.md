# UI Schema & Contract Migration

**Status:** Retrospective completed epic grouping.

This folder groups the completed UI compatibility work that adapted the legacy Blazor UI to evolved Engine and Sim contracts, especially schema changes around time-grid representation.

## Themes

- Migrate UI clients to updated Engine and Sim response contracts.
- Preserve compatibility while removing legacy assumptions from UI parsing logic.
- Capture the completed implementation log beside the milestone spec.

## Milestones

- `UI-M-02.09` — UI schema migration for `binSize` / `binUnit` support.

## Notes

- Historical archive docs also reused the `UI-M-02.09` identifier for an older compare-workflow concept.
- This retrospective grouping follows the shipped milestone document and log that implement schema migration, not the earlier abandoned meaning.