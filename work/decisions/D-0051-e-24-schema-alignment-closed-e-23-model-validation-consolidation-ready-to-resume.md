---
id: D-0051
title: E-0024 Schema Alignment closed; E-0023 Model Validation Consolidation ready to resume
status: accepted
---

**Status:** active
**Context:** E-0024 ran five milestones across two days (started 2026-04-24, closed 2026-04-25). Each milestone landed all of its ACs as drafted; the final canary survey collapsed from 726 → 0 validator errors at `ValidationTier.Analyse` across all twelve shipped templates. M-0053 (final milestone) promoted `tests/FlowTime.Integration.Tests/TemplateWarningSurveyTests.cs::Survey_Templates_For_Warnings` from an informational diagnostic into a hard-asserting build-time gate: a non-zero `val-err` count from the in-process tier-3 validator now fails the build. Regression-catching was verified end-to-end by transient un-wire of M-0052's `QuotedAmbiguousStringEmitter` — the canary failed with 231 errors across all twelve templates, exactly matching the M-0052 pre-fix baseline; restoring the wiring returned the canary to green. The full `.NET` solution suite remained green (1,749 passed / 9 skipped pre-existing / 0 failed) throughout. All three AC4 grep audits (snake_case provenance keys in `model.schema.yaml`; `SimModelArtifact` in C# sources; `SimNode`/`SimOutput`/`SimProvenance`/`SimTraffic`/`SimArrival`/`SimArrivalPattern` in C# sources) report zero in-tree hits. `docs/schemas/model.schema.md` was the one stale active doc remaining post-m-E24-03; its provenance section was rewritten to the canonical 7-field nested camelCase form ratified in M-0049 Q5/A4 and a historical note was added explicitly naming the deprecated snake_case keys for reader continuity. `docs/schemas/README.md` and `docs/architecture/run-provenance.md` (both updated by M-0051) were verified current.

**Decision:** Close E-0024 Schema Alignment. Flip E-0023 Model Validation Consolidation from `paused` to `ready-to-resume`. The original three-milestone E-0023 plan (M-0046 Schema Alignment / M-0047 Call-Site Migration / M-0048 Delete `ModelValidator`) is preserved with one substantive change: M-0046's work has been largely absorbed by E-0024 (specifically by M-0051 schema rewrite and M-0053 canary commit + hard-assertion promotion); M-0047 and M-0048 are now byte-trivial mechanical cleanup because `ModelSchemaValidator`, the schema, Sim's emitter, and the Engine's reader literally share a single unified type definition (`ModelDto` / `ProvenanceDto` in `FlowTime.Contracts`). E-0023 reentry needs a status-reconciliation pass on the `milestone/m-E23-01-schema-alignment` branch's stashed input material before M-0047 starts: re-examine the three uncommitted schema edits (`grid.start`, `nodes[].metadata`, `nodes[].source`) — `grid.start` and `nodes[].metadata` have landed via M-0051; `nodes[].source` was deliberately omitted per Q4 (forward contract for E-0015); reconcile the M-0046 spec frontmatter to reflect this absorption; then start M-0047 from the appropriate branch tip. No deltas were observed between M-0049's design decisions and the final landed state — every Q1–Q6 / A1–A5 ratification held through implementation, including `outputs[].as` optional (Q3), `nodes[].source` absent (Q4), nested 7-field camelCase provenance (Q5/A4), and `ModelDto` in `FlowTime.Contracts` (Q1). All five E-0024 ADRs ratified during their owning milestones (ADR-E-0024-01/02/03 in M-0049; ADR-E-0024-04/05 in M-0052).

**Consequences:** E-0024 enters the completed-but-not-yet-archived state — the integration branch `epic/E-0024-schema-alignment` will be merged to `main` separately (next deployer-scope action) and only then archived to `work/epics/completed/E-0024-schema-alignment/` per project rule. E-0023 status surfaces flip in one pass: epic spec frontmatter `paused` → `ready-to-resume`, milestone-table entries flipped to `ready` for M-0047 / M-0048, ROADMAP and epic-roadmap E-0023 sections updated. CLAUDE.md Current Work flips E-0024 to in-progress-with-m-E24-05-final and E-0023 to ready-to-resume. The canary is now live as a regression gate — any future schema, emitter, or validator change that re-introduces validator errors at `ValidationTier.Analyse` on any shipped template fails CI immediately; the diagnostic message names every offender with template id, error count, and a sample first error. Downstream: M-0044 Validation Surface (Svelte) becomes ready when E-0023 closes; E-0015 Telemetry Ingestion starts from the ratified `nodes[].source` forward contract rather than inheriting a forward-declared placeholder (M-0049 Q4). The 2026-04-23 Truth Discipline guard (*"'API stability' does not mean 'keep old functions around.'"*) was applied through E-0024 at every relevant seam: `SimModelArtifact` and its six satellites deleted outright (no shim); the snake_case provenance keys not retained as aliases; `ModelDefinition` collapsed into `ModelDto` rather than preserved alongside; bundle migration explicitly forward-only with no compatibility reader.
