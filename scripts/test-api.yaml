grid:
  bins: 3
  binSize: 1
  binUnit: hours
nodes:
  - id: demand
    kind: const
    values: [10, 20, 30]
  - id: capacity
    kind: const
    values: [25, 25, 25]
  - id: flow
    kind: expr
    expression: min(demand, capacity)
