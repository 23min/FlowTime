grid:
  bins: 3
  binMinutes: 60
nodes:
  - id: base
    kind: const
    values: [100, 200, 300]
  - id: result
    kind: expr
    expr: "(base * 2) + SHIFT(base, 1)"
outputs:
  - series: result
    as: final
