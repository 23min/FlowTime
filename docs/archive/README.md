# Archive — Historical Reference Only

**DO NOT use these documents for current state.** They are preserved for historical context only.

These files describe superseded roadmaps, deprecated charters, legacy architecture, and old coordination matrices. They reflect what the project looked like at specific points in the past, not what it is now.

## For AI agents

- Do not treat any file in this directory as implementation authority.
- Do not cite these documents as current architecture or contracts.
- If a search leads you here, look for the current equivalent in `docs/` (ground truth) or `work/epics/` (decided-next direction).

## Truth precedence reminder

1. Code + passing tests define live truth
2. `work/decisions.md` defines approved direction
3. Milestone and epic specs define implementation target
4. `docs/` architecture docs summarize and connect the above
5. **This archive is context only**
