# M-09.01 - Engine Semantics Layer (Contract Hardening) — Tracking

**Status:** 🟢 Ready to Wrap (with deferred items)

## Phase 1: Semantics Inventory + Docs (Done)
### Tests (write first)
- [x] Add/extend golden tests for `/state` and `/state_window` semantics coverage.
- [ ] Add warning emission tests for missing-input behavior. (Deferred)
- [x] Verify metadata fields (origin/aggregation) in API payloads.

### Implementation Tasks
- [x] Build a semantics catalog for all node and edge series (units, origin, aggregation, gating).
- [x] Align `docs/architecture/engine-semantics-layer/README.md` with the catalog.
- [x] Cross-check `docs/schemas/` and UI provenance docs for consistency (edge metadata + schema alignment).
- [x] Document warning families and triggers.

## Phase 2: Contract Hardening Tests (Deferred)
### Tests (write first)
- [ ] Add goldens for service, serviceWithBuffer, router, sink node kinds.
- [ ] Add goldens for edge metrics (flow/attempt/retry volumes + quality).
- [ ] Add null + warning assertions to prevent silent fallbacks.

### Implementation Tasks
- [ ] Ensure warnings are persisted in run artifacts and surfaced consistently.

## Phase 3: Consumer Contract Notes (Deferred)
### Tests (write first)
- [ ] N/A (documentation-only phase).

### Implementation Tasks
- [ ] Document derived vs explicit fields and null-handling rules.
- [ ] Update MCP and UI docs with pass-through guidance.
- [ ] Add examples for warning interpretation and quality flags.

## Notes
- Run `dotnet build` and per-project tests before handoff.
- No client-side derivation; engine outputs are authoritative.
- Tests run (per project):
  - `dotnet test tests/FlowTime.Api.Tests/FlowTime.Api.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Core.Tests/FlowTime.Core.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Expressions.Tests/FlowTime.Expressions.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Generator.Tests/FlowTime.Generator.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Adapters.Synthetic.Tests/FlowTime.Adapters.Synthetic.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Sim.Tests/FlowTime.Sim.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Cli.Tests/FlowTime.Cli.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.UI.Tests/FlowTime.UI.Tests.csproj --nologo`
  - `dotnet test tests/FlowTime.Tests/FlowTime.Tests.csproj --nologo` (perf tests skipped)
  - `dotnet test tests/FlowTime.Integration.Tests/FlowTime.Integration.Tests.csproj --nologo`
