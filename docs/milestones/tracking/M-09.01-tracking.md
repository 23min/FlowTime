# M-09.01 - Engine Semantics Layer (Contract Hardening) — Tracking

**Status:** 📋 Planned

## Phase 1: Semantics Inventory + Docs (Planned)
### Tests (write first)
- [ ] Add/extend golden tests for `/state` and `/state_window` semantics coverage.
- [ ] Add warning emission tests for missing-input behavior.
- [ ] Verify metadata fields (origin/aggregation) in API payloads.

### Implementation Tasks
- [ ] Build a semantics catalog for all node and edge series (units, origin, aggregation, gating).
- [ ] Align `docs/architecture/engine-semantics-layer/README.md` with the catalog.
- [ ] Cross-check `docs/schemas/` and UI provenance docs for consistency.
- [ ] Document warning families and triggers.

## Phase 2: Contract Hardening Tests (Planned)
### Tests (write first)
- [ ] Add goldens for service, serviceWithBuffer, router, sink node kinds.
- [ ] Add goldens for edge metrics (flow/attempt/retry volumes + quality).
- [ ] Add null + warning assertions to prevent silent fallbacks.

### Implementation Tasks
- [ ] Wire any missing metadata fields into API payloads.
- [ ] Ensure warnings are persisted in run artifacts and surfaced consistently.

## Phase 3: Consumer Contract Notes (Planned)
### Tests (write first)
- [ ] N/A (documentation-only phase).

### Implementation Tasks
- [ ] Document derived vs explicit fields and null-handling rules.
- [ ] Update MCP and UI docs with pass-through guidance.
- [ ] Add examples for warning interpretation and quality flags.

## Notes
- Run `dotnet build` and per-project tests before handoff.
- No client-side derivation; engine outputs are authoritative.
