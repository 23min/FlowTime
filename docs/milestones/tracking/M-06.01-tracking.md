# M-06.01 Implementation Tracking

> **Note:** This tracking document is created when work begins on the feature branch.
> Status remains 📋 Planned until coding starts; update to 🔄 In Progress once Phase 1 implementation begins.

**Milestone:** M-06.01 — Evaluation Integrity (DAG/Spreadsheet Contract)  
**Started:** 2026-01-17  
**Status:** 🔄 In Progress  
**Branch:** `milestone/m-06.01`  
**Assignee:** Codex (paired with user)

---

## Quick Links

- **Milestone Document:** `docs/milestones/M-06.01-evaluation-integrity-dag-contract.md`
- **Architecture Epic:** `docs/architecture/evaluation-integrity/README.md`
- **Milestone Guide:** `docs/development/milestone-documentation-guide.md`

---

## Current Status

### Overall Progress
- [x] Phase 1: Contract + Inventory (3/3 tasks)
- [x] Phase 2: Engine Refactor (3/3 tasks)
- [x] Phase 3: Guardrails + Diagnostics (3/3 tasks)
- [ ] Phase 4: Tests + Validation (1/3 tasks)

### Test Status
- **Full Suite:** `dotnet test --nologo` ✅ (perf tests skipped; existing CS8618/CS0169 warnings)

---

## Progress Log

### 2026-01-17 - Session Start

**Preparation:**
- [x] Read milestone document
- [x] Read related documentation
- [x] Create epic integration branch (`epic/evaluation-integrity`)
- [x] Create milestone branch (`milestone/m-06.01`)
- [x] Create tracking document

**Next Steps:**
- [ ] Begin Phase 1
- [ ] Start with inventory of evaluation pipeline + override paths

---

### 2026-01-17 - Phase 1 Inventory + Contract

**Changes:**
- Documented evaluation pipeline and override sources in `docs/milestones/tracking/M-06.01-tracking.md`.
- Added current pipeline + target contract notes in `docs/architecture/evaluation-integrity/README.md`.
- Mapped test coverage targets to acceptance criteria in the tracking doc.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterClassSeriesOverrideTests` (RED)
  - Failing: `WriteArtifactsAsync_PropagatesClassSeriesOverridesToDerivedNodes`
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Begin Phase 2 with RED tests for class propagation ordering.

---

### 2026-01-17 - Phase 2 RED Test

**Changes:**
- Added RED test for class series override propagation in `tests/FlowTime.Core.Tests/Artifacts/RunArtifactWriterClassSeriesOverrideTests.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterClassSeriesOverrideTests` (fails as expected).

**Next Steps:**
- [ ] Implement pre-eval class series overrides so derived nodes recompute correctly.

---

### 2026-01-17 - Phase 2 GREEN (Overrides Integrated)

**Changes:**
- Integrated class series overrides into the class contribution DAG pipeline (`src/FlowTime.Core/Artifacts/ClassContributionBuilder.cs`).
- Switched `RunArtifactWriter` to feed overrides into the pre-eval pass instead of mutating after build (`src/FlowTime.Core/Artifacts/RunArtifactWriter.cs`).

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterClassSeriesOverrideTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Proceed to Task 2.2 (refactor class evaluation to DAG-ordered pipeline for router/SWB/topology overrides).

---

### 2026-01-17 - Phase 2 Refactor Pass

**Changes:**
- Reworked class contribution evaluation to rebuild from a clean pipeline after each override stage instead of mutating in-place.
- Added override merge helpers so router/SWB/topology overrides feed the pre-eval map.

**Tests:**
- `dotnet test --nologo --filter "FullyQualifiedName~RouterClassContributionTests|FullyQualifiedName~TopologyClassContributionTests"` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterClassSeriesOverrideTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Continue Task 2.2 (remove remaining post-eval injection paths).

---

### 2026-01-17 - Phase 2 RED Test (Queue Depth)

**Changes:**
- Added RED test to enforce queue depth derivation via DAG (includes loss + initial condition) in `tests/FlowTime.Core.Tests/Artifacts/RunArtifactWriterDerivedSeriesTests.cs`.

**Tests:**
- Not run yet (expected to fail under post-eval injection path).

**Next Steps:**
- [ ] Refactor `RunArtifactWriter` to compute derived queue depth via compiled DAG evaluation.

---

### 2026-01-17 - Phase 2 GREEN (Derived Series via DAG)

**Changes:**
- Replaced post-eval queue depth/retry echo injection with compile-to-DAG expansion in `src/FlowTime.Core/Artifacts/RunArtifactWriter.cs`.
- Added queue depth node synthesis (serviceWithBuffer) and retry echo expression synthesis for missing series.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterDerivedSeriesTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Continue Phase 2 Task 2.3 (integration tests).

---

### 2026-01-17 - Phase 2 GREEN (Integration Tests)

**Changes:**
- Added template-level integration tests in `tests/FlowTime.Sim.Tests/Templates/EvaluationIntegrityTemplateTests.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~EvaluationIntegrityTemplateTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Begin Phase 3 guardrails + diagnostics.

---

### 2026-01-17 - Phase 3 RED Test (Late Injection Guard)

**Changes:**
- Added RED test for post-eval injection detection in `tests/FlowTime.Core.Tests/Analysis/InvariantAnalyzerTests.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~InvariantAnalyzerTests` (RED)

---

### 2026-01-17 - Phase 3 GREEN (Late Injection Guard)

**Changes:**
- Added post-eval injection warning in `src/FlowTime.Core/Analysis/InvariantAnalyzer.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~InvariantAnalyzerTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Continue Phase 3 Task 3.2 (topology class coverage warnings).

## Phase 1: Contract + Inventory

**Goal:** Document the evaluation contract and identify all post-eval mutation/override paths.

### Task 1.1: Inventory evaluation pipeline + override paths
**File(s):** TBD (identify engine eval + class propagation pipeline)

**Checklist (TDD Order - Tests FIRST):**
- [x] N/A (analysis-only)
- [x] Document evaluation pipeline and mutation points

**Findings (pipeline + override paths):**
- `src/FlowTime.Core/Routing/RouterAwareGraphEvaluator.cs` runs a two-pass evaluation: `Graph.Evaluate` then `Graph.EvaluateWithOverrides` using overrides from `RouterFlowMaterializer.ComputeOverrides` (full recompute path for totals).
- `src/FlowTime.Core/Routing/RouterFlowMaterializer.cs` builds router overrides and uses `ClassContributionBuilder.Build` to compute class series for class-aware routing.
- `src/FlowTime.Core/Artifacts/ClassContributionBuilder.cs` builds class series in DAG order, then applies router/serviceWithBuffer/topology overrides and **recomputes** contributions after each override batch (post-eval mutation + partial recompute risk).
- `src/FlowTime.Core/Artifacts/RunArtifactWriter.cs` mutates the evaluated totals context after evaluation:
  - `TryPrecomputeQueueDepthSeries` injects queue series from semantics + arrivals/served.
  - `TryPrecomputeRetryEchoSeries` injects retry echo series from failures/errors + retry kernel.
  - `ClassSeriesOverride` allows external class series injection **after** class contributions are built.
- `src/FlowTime.Core/Analysis/InvariantAnalyzer.cs` and `src/FlowTime.Sim.Core/Analysis/TemplateInvariantAnalyzer.cs` both call `ClassContributionBuilder.Build`, so any post-eval mutation in that pipeline affects diagnostics and warning fidelity.

**Status:** ✅ Completed

---

### Task 1.2: Define pre-eval override pipeline contract
**File(s):** `docs/architecture/evaluation-integrity/README.md` (update if needed)

**Checklist (TDD Order - Tests FIRST):**
- [x] N/A (doc-only)
- [x] Specify pre-eval override ordering + immutability rules

**Notes:**
- Documented current evaluation pipeline, target contract, and override sources in `docs/architecture/evaluation-integrity/README.md`.
- Added compile-to-DAG note clarifying internal node expansion vs. UI node model.

**Status:** ✅ Completed

---

### Task 1.3: Map tests to acceptance criteria
**File(s):** `tests/FlowTime.Sim.Tests/` (targets TBD)

**Checklist (TDD Order - Tests FIRST):**
- [x] N/A (planning-only)
- [x] Finalize test list + scope per acceptance criteria

**Planned Tests (mapping to acceptance criteria):**
- FR1 (single pipeline / no post-eval mutation):
  - `ClassContributionPipeline_RejectsPostEvalMutation` (new, FlowTime.Core.Tests)
  - `RunArtifactWriter_RejectsLateClassSeriesOverride` (new, FlowTime.Core.Tests)
- FR2 (class propagation uses DAG order):
  - `ClassContributionPipeline_RecomputesDerivedNodesOnOverrides` (new, FlowTime.Core.Tests)
  - Extend `TopologyClassContributionTests` to include derived expr chains (FlowTime.Core.Tests)
- FR3 (order violation guardrails):
  - `InvariantAnalyzer_FailsOnPostEvalClassMutation` (new, FlowTime.Core.Tests)
  - `InvariantAnalyzer_WarnsOnTopologyClassCoverageGaps` (existing coverage, verify)
- FR4 (regression coverage across nodes):
  - `TransportationTemplate_ClassPropagation_LeavesNoDerivedGaps` (new, FlowTime.Sim.Tests)
  - `RouterOverrides_DoNotBypassDagOrder` (new, FlowTime.Sim.Tests)

**Status:** ✅ Completed

---

## Phase 2: Engine Refactor

**Goal:** Move class propagation + overrides into a single DAG-ordered evaluation pass.

### Task 2.1: Add RED tests for class propagation ordering
**File(s):** `tests/FlowTime.Core.Tests/Artifacts/RunArtifactWriterClassSeriesOverrideTests.cs`

**Checklist (TDD Order - Tests FIRST):**
- [x] Write failing unit test: `WriteArtifactsAsync_PropagatesClassSeriesOverridesToDerivedNodes` (RED - added, not run)
- [x] Implement engine changes to make test pass

**Status:** ✅ Completed

---

### Task 2.2: Refactor class evaluation to DAG-ordered pipeline
**File(s):** TBD (engine eval + class propagation pipeline)

**Checklist (TDD Order - Tests FIRST):**
- [x] Ensure overrides are applied before evaluation
- [x] Remove/forbid post-eval injection paths
- [x] Keep router/serviceWithBuffer/topology semantics in DAG order

**Notes:**
- Refactored class contribution evaluation to recompute from a clean pipeline after each override stage.
- Overrides now merge into a pre-eval override map rather than mutating the contributions in-place.
- Removed post-eval queue depth + retry echo injections by compiling derived series into the DAG.

**Status:** ✅ Completed

---

### Task 2.3: Add RED integration tests for template behavior
**File(s):** `tests/FlowTime.Sim.Tests/` (targets TBD)

**Checklist (TDD Order - Tests FIRST):**
- [x] Write integration tests:
  - `TransportationTemplate_ClassPropagation_LeavesNoDerivedGaps`
  - `TransportationTemplate_RouterOverrides_DoNotLeakClasses`
- [x] Implement supporting fixes as needed

**Status:** ✅ Completed

---

## Phase 3: Guardrails + Diagnostics

**Goal:** Fail fast on late mutation and surface clear diagnostics.

### Task 3.1: Add late-mutation detection guard
**File(s):** TBD (engine eval + analyzer diagnostics)

**Checklist (TDD Order - Tests FIRST):**
- [x] Write failing analyzer test: `Analyze_WarnsWhenEvaluatedSeriesContainsUnknownNodes`
- [x] Implement guard (warning)

**Status:** ✅ Completed

---

### Task 3.2: Extend invariant warnings for topology semantics gaps
**File(s):** TBD (analyzer warnings)

**Checklist (TDD Order - Tests FIRST):**
- [x] Write failing analyzer test: `Analyze_WarnsOnTopologyClassCoverageGaps`
- [x] Implement warning + remediation guidance

**Status:** ✅ Completed

---

### 2026-01-17 - Phase 3 GREEN (Topology Class Coverage)

**Changes:**
- Added topology class coverage warnings for non-serviceWithBuffer nodes in `src/FlowTime.Core/Analysis/InvariantAnalyzer.cs`.
- Added analyzer test `Analyze_WarnsOnTopologyClassCoverageGaps` in `tests/FlowTime.Core.Tests/Analysis/InvariantAnalyzerTests.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~InvariantAnalyzerTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Continue Phase 3 Task 3.3 (diagnostics surfacing).

---

### Task 3.3: Ensure diagnostics surface in run manifests/UI
**File(s):** TBD (diagnostics pipeline)

**Checklist (TDD Order - Tests FIRST):**
- [x] Add test or validation hook to confirm diagnostic surfacing
- [x] Wire diagnostics into run manifest + UI warning list

**Status:** ✅ Completed

---

### 2026-01-17 - Phase 3 GREEN (Run Warning Surfacing)

**Changes:**
- Added run.json warning surfacing test in `tests/FlowTime.Core.Tests/Artifacts/RunArtifactWriterWarningTests.cs`.

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterWarningTests` ✅
  - Warnings: filter not matched in other test projects; existing warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs` (CS8618, CS0169)

**Next Steps:**
- [ ] Begin Phase 4 tests + validation.

---

### 2026-01-17 - Phase 4 Regression + Coverage Alignment

**Changes:**
- Normalized const series to the request grid during effective-context evaluation to prevent mismatched lengths in ad-hoc artifact runs (`src/FlowTime.Core/Artifacts/RunArtifactWriter.cs`).
- Added class coverage override passthrough from telemetry capture into run artifacts (`src/FlowTime.Generator/TelemetryBundleBuilder.cs`, `src/FlowTime.Core/Artifacts/RunArtifactWriter.cs`).
- Resolved state metadata classCoverage by merging computed coverage with manifest coverage (`src/FlowTime.API/Services/StateQueryService.cs`).
- Updated class coverage expectations for loop parity + template diagnostics (`tests/FlowTime.Integration.Tests/Loop/ClassesLoopTests.cs`, `tests/FlowTime.Sim.Tests/Templates/EvaluationIntegrityTemplateTests.cs`).

**Tests:**
- `dotnet test --nologo --filter FullyQualifiedName~RunArtifactWriterTests` ✅
- `dotnet test --nologo --filter FullyQualifiedName~ClassesLoopTests.MissingClassTelemetryEmitsWarningsAndPartialCoverage` ✅
- `dotnet test --nologo --filter FullyQualifiedName~EvaluationIntegrityTemplateTests.TransportationTemplate_ClassPropagation_LeavesNoDerivedGaps` ✅
- `dotnet build` ✅ (CS8618/CS0169 warnings in `tests/FlowTime.Sim.Tests/Service/TemplateGenerateProvenanceTests.cs`)
- `dotnet test --nologo` ✅ (perf tests skipped; existing CS8618/CS0169 warnings)

**Next Steps:**
- [ ] Manual UI verification (user-led): validate class filter behavior and warning coverage in the transportation templates.

---

## Phase 4: Tests + Validation

**Goal:** Lock contract via tests and validate end-to-end behavior.

### Task 4.1: Full regression suite
**Checklist (TDD Order - Tests FIRST):**
- [x] `dotnet build`
- [x] `dotnet test --nologo`

**Status:** ✅ Complete

---

### Task 4.2: Manual verification notes
**Checklist (TDD Order - Tests FIRST):**
- [ ] Validate class propagation across serviceWithBuffer -> router -> service chains
- [ ] Confirm no late mutation warnings in expected templates

**Status:** ⏳ Pending (user-led)

---

### Task 4.3: Milestone wrap preparation
**Checklist (TDD Order - Tests FIRST):**
- [x] Update tracking doc with results + tests
- [ ] Prepare release notes entry (if applicable)

**Status:** 🔄 In Progress
