# M-08.05 - MCP Edge Metrics Support

**Status:** Complete  
**Dependencies:** M-07.04 (Edge Semantics Contract), M-07.05 (Derived Path Latency)  
**Target:** Extend MCP analyst tooling to expose edge-level metrics and quality signals from the time-travel API.

---

## Overview

Edge metrics (edge time bins, edge quality, and edge warnings) are now part of the FlowTime contract under the edge-time-bin epic. The MCP server currently exposes only node-centric state. This milestone updates MCP schemas and tooling so AI clients can query and interpret edge series without re-deriving anything client-side.

### Strategic Context
- **Motivation:** MCP clients should consume the same engine-owned edge semantics as the UI.
- **Impact:** Enables edge-aware analysis, conservation checks, and retry-load interpretation in MCP workflows.
- **Dependencies:** Requires stable edge semantics and quality definitions from the edge-time-bin epic.

---

## Scope

### In Scope
1. MCP tool schema updates to include edge series in `get_state_window`.
2. Edge quality and edge warning metadata surfaced in MCP responses.
3. Derived sink/path latency series passed through from node series.
4. Server-side edge filters (`edgeIds`, `edgeMetrics`, `classIds`) added to `/state_window` and passed through by MCP tools.
5. Documentation updates for MCP analyst workflow and tool schemas.

### Out of Scope
- New edge computations or inference in MCP (engine remains source of truth).
- UI changes or new UI endpoints.
- Path-level analytics beyond the edge-time-bin contract.
- Path analysis/path filters or subgraph extraction (tracked in architecture gaps).
- Summary helpers that derive new analytics (planned follow-up milestone if needed).

---

## Requirements

### Functional Requirements

#### FR1: Edge Series Exposure
**Description:** MCP `get_state_window` returns edge metrics when present in the run.

**Acceptance Criteria:**
- [ ] Edge series are included alongside node series when available.
- [ ] Edge series are keyed by `edgeId` and optionally `classId`.
- [ ] Tool response makes it explicit when edge series are missing or approximate.

#### FR2: Edge Quality + Warnings
**Description:** Edge quality and edge warning signals are included in MCP outputs.

**Acceptance Criteria:**
- [ ] `metadata.edgeQuality` is surfaced in MCP outputs for `/state` and `/state_window`.
- [ ] `edgeWarnings` are surfaced alongside edge series when present.

#### FR3: Derived Path Latency (Pass-Through)
**Description:** MCP passes through derived sink/path latency series from node series (no new computation).

**Acceptance Criteria:**
- [ ] `pathLatencyMs` (or the derived sink latency series) is visible in MCP `get_state_window` node series when present in the API payload.
- [ ] MCP does not compute or alter derived latency values.

#### FR4: Edge Filters (Server-Side)
**Description:** `/state_window` supports edge-specific filters, and MCP tools pass them through.

**Acceptance Criteria:**
- [ ] `/state_window` accepts `edgeIds`, `edgeMetrics`, and `classIds` query parameters.
- [ ] MCP passes edge filters through to the API.
- [ ] Edge filters reduce payloads without changing node series semantics.

### Non-Functional Requirements

#### NFR1: No Client-Side Derivation
**Target:** MCP server never infers edge metrics; it only forwards engine output.
**Validation:** Tool responses map 1:1 to API payloads.

---

## Implementation Plan

### Phase 1: Tool Schema + Docs
**Goal:** Define MCP schema changes and update analyst workflow docs.

**Tasks:**
1. Update MCP tool schemas to allow edge series.
2. Document edge metrics and quality fields in MCP analyst docs.
3. Add explicit dependency notes on edge-time-bin semantics.
4. Document derived sink/path latency as a pass-through series.

**Success Criteria:**
- [ ] MCP documentation lists edge metric fields and filters.

### Phase 2: MCP Server + API Wiring
**Goal:** Pass through edge filters and return edge series in tool responses.

**Tasks:**
1. Add API support for `edgeIds`, `edgeMetrics`, and `classIds` on `/state_window`.
2. Update MCP server `get_state_window` handler to accept edge filters.
3. Map API response edge sections into MCP tool output.
4. Map `metadata.edgeQuality` and `edgeWarnings` into MCP output.
5. Add contract tests for edge-series presence and derived latency pass-through.

**Success Criteria:**
- [ ] MCP `get_state_window` returns edge series for edge-enabled runs.

---

## Test Plan

### Unit Tests
**Focus:** Tool schema validation for edge filters.

### Integration Tests
**Focus:** MCP server returns edge series, edge quality/warnings, and derived latency for runs that include edge data.

---

## Success Criteria

### Milestone Complete When:
- [ ] MCP docs and tool schemas include edge metrics + quality.
- [ ] MCP server returns edge series without client-side inference.
- [ ] Integration tests cover edge series responses.
